import "./footer.css";
import ImageController from "../../controls/image";

function footer(props) {
  return (
    <div className="footer">
      {/* <ImageController url={props.url} alt={props.alt}/> */}
      <p>Developed by Gayan Wimalarathna</p>
    </div>
  );
}

export default footer;
